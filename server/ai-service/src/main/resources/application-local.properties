# AI Service Local Configuration
server.port=${SERVER_PORT}

# Application name
spring.application.name=ai-service

# gRPC Client Configuration - Using localhost for local development
grpc.client.cv-service.address=${GRPC_CLIENT_CV_SERVICE_ADDRESS}
grpc.client.cv-service.negotiationType=PLAINTEXT

# RAG Configuration
rag.chunk-size=1000
rag.chunk-overlap=200
rag.top-k=5

# Actuator endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Logging
logging.level.com.example.aiservice=DEBUG
logging.level.org.springframework.ai=DEBUG

# Allow bean definition overriding
spring.main.allow-bean-definition-overriding=true
